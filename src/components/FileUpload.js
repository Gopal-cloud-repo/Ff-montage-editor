import React, { useState } from 'react';\n\nconst FileUpload = () => {\n  const [files, setFiles] = useState([]);\n\n  const handleDrop = (event) => {\n    event.preventDefault();\n    const acceptedFiles = Array.from(event.dataTransfer.files).filter((file) =>\n      file.type.startsWith('audio/') || file.type.startsWith('video/')\n    );\n    setFiles((prevFiles) => [...prevFiles, ...acceptedFiles]);\n  };\n\n  const handleDragOver = (event) => {\n    event.preventDefault();\n  };\n\n  return (\n    <div\n      onDrop={handleDrop}\n      onDragOver={handleDragOver}\n      style={{ height: '300px', border: '2px dashed #ccc', display: 'flex', alignItems: 'center', justifyContent: 'center' }}\n    >\n      <p>Drag and drop video or audio files here</p>\n    </div>\n  );\n};\n\nexport default FileUpload;